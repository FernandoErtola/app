/* const Carrito = ({items}) => {
    return items.map(i => {
        return <>
            <div><span>{i.name}</span></div>
        </>
    })
} */


const Carrito = () => {
    return(
        <div>
            <h1>Carrito</h1>
        </div>
    )
}

export default Carrito;