const Navbar = () => {
    return(
        <nav className="navBar">
                <a href="#">HOME</a>
                <a href="#">PRODUCTOS</a>
                <a href="#">DESTACADOS</a>
                <a href="#">CONTACTO</a>
            </nav>
    )
} 

export default Navbar